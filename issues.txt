
DONE 2. There is as yet not method for chunking the generation of data-objects across prompts to 
improve stability for the resulting C# data-object code.
3. API json_names may not be making it into the final prompt. (Which begs the question for how they are being assigned by LLM in the final code?)

output directories should probably be placed in a unified out directory instead of the main workspace
config.toml should be in the directory which contains the codebase that it configs

4. nested types do not always seem to make it into the parent type post-gen as 
documented in prompt packets.